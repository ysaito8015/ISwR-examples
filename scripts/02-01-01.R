rm(list = ls())
gc()

bmi <- d <- exp.lean <- exp.obese <- fpain <- height <- NA
hh <- intake.post <- intake.pre <- l <- m <- NA
mylist <- o <- oops <- pain <- sel <- v <- NA
weight <- x <- xbar <- y <- NA

ls()
rm(height, weight)
cat("\n\n")
ls()
cat("\n\n")

print("save.image()")
save.image()
